"""Trip Planner domain package (services, future blueprints).

The Flask application instance lives in the top-level ``app.py`` module (the
``app`` name is reserved for that module until a factory split).

Needs: REQ-005, ADR-0001
"""
